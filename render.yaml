services:
  - type: web
    name: health-metric-app             # 🔁 Your app's name on Render
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate          # 🛠 This runs migrations automatically
    startCommand: gunicorn BMI_calculator.wsgi      # 🔁 Replace if your project is named differently
    envVars:
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        value: 4om%rc413!hz@6xjcsykpzu88%1p696q@)-kl3^5tgl8hosu24          # 🔁 Replace with your real secret
      - key: DATABASE_URL
        value: postgresql://healthuser:R73zfkFXJ4TQ5y1Oe40l28O08iQwm9WY@dpg-d1qmb27fte5s73dh7820-a/healthmetric_db        # 🔁 Paste from Render’s PostgreSQL “Internal DB URL”
      - key: ALLOWED_HOSTS
        value: health-metric-app.onrender.com
